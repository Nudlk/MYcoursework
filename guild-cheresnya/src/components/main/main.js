import '../main/main.css'
export default (props) => {
    return (
        <>
        <dt>{props.title}</dt>
        <dd>{props.Rule1}</dd>
        <dd>{props.Rule2}</dd>
        <dd>{props.Rule3}</dd>
        <dd>{props.Rule4}</dd>
        <dd>{props.Rule5}</dd>
        </>
    )
}